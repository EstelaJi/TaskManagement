# 📋 Task Management - 个人任务管理系统

一个基于 Vue 3 和 Node.js 的任务管理应用

## ✨ 项目特点

- 🎯 **Vue 3 Composition API** - 使用最新的组合式 API 开发
- 🎨 **Tailwind CSS** - 实用优先的 CSS 框架，快速构建美观界面
- 🚀 **Vite** - 极速的前端构建工具
- 📦 **模块化设计** - 清晰的代码结构，易于学习和扩展
- 🔄 **RESTful API** - 标准的后端 API 设计

## 🛠️ 技术栈

### 前端
- **Vue 3** - 渐进式 JavaScript 框架（Composition API）
- **Vue Router 4** - 官方路由管理器
- **Vite** - 下一代前端构建工具
- **Tailwind CSS** - 实用优先的 CSS 框架
- **Axios** - HTTP 客户端

### 后端
- **Node.js** - JavaScript 运行时
- **Express** - Web 应用框架
- **CORS** - 跨域资源共享

## 📋 前置要求

在开始之前，请确保你的开发环境已安装：

- **Node.js** >= 16.0.0
- **npm** >= 7.0.0 或 **yarn** >= 1.22.0

你可以通过以下命令检查版本：

```bash
node -v
npm -v
```

## 🚀 快速开始

### 1. 克隆项目（如果是从 Git 仓库）

```bash
git clone <repository-url>
cd TaskManagement
```

### 2. 安装依赖

#### 方式一：一键安装所有依赖（推荐）

在项目根目录运行：

```bash
npm install
npm run install
```

这会自动安装根目录、前端和后端的所有依赖。

#### 方式二：分别安装依赖

如果你想分别安装：

**安装前端依赖**

```bash
cd frontend
npm install
```

**安装后端依赖**

```bash
cd ../backend
npm install
```

### 3. 配置环境变量（可选）

后端项目支持环境变量配置，你可以创建 `.env` 文件：

```bash
cd backend
cp .env.example .env
```

编辑 `.env` 文件（默认配置已足够）：

```env
PORT=3001
NODE_ENV=development
```

### 4. 运行项目

#### 方式一：一键启动（推荐）✨

在项目根目录运行：

```bash
npm run dev
```

这个命令会同时启动前端和后端服务：

- **后端服务**：`http://localhost:3001`
- **前端应用**：`http://localhost:3000`

你会在终端看到两个服务的输出，使用 `Ctrl+C` 可以同时停止两个服务。

#### 方式二：分别启动前后端

如果你想分别控制前后端：

**启动后端服务**

打开第一个终端窗口：

```bash
cd backend
npm run dev
```

或者从根目录：

```bash
npm run dev:backend
```

**启动前端开发服务器**

打开第二个终端窗口：

```bash
cd frontend
npm run dev
```

或者从根目录：

```bash
npm run dev:frontend
```

#### 方式三：使用生产模式

**构建前端**

```bash
cd frontend
npm run build
```

**启动后端（生产模式）**

```bash
cd backend
npm start
```

**预览前端构建结果**

```bash
cd frontend
npm run preview
```

在项目根目录可以使用以下命令：

```bash
# 一键安装所有依赖
npm run install

# 一键启动前后端（开发模式）
npm run dev

# 只启动前端
npm run dev:frontend

# 只启动后端
npm run dev:backend

# 构建前端生产版本
npm run build

# 启动后端（生产模式）
npm start

# 预览前端构建结果
npm run preview
```

### 前端命令

```bash
cd frontend

# 开发模式（热重载）
npm run dev

# 构建生产版本
npm run build

# 预览生产构建
npm run preview
```

### 后端命令

```bash
cd backend

# 开发模式（自动重启）
npm run dev

# 生产模式
npm start
```

## 🌐 API 接口

后端提供以下 RESTful API：

### 任务相关接口

- `GET /api/tasks` - 获取所有任务
- `GET /api/tasks/:id` - 获取单个任务
- `POST /api/tasks` - 创建新任务
- `PATCH /api/tasks/:id` - 更新任务
- `DELETE /api/tasks/:id` - 删除任务

### 健康检查

- `GET /api/health` - 服务器健康状态

## 🎓 学习资源

### Vue 3 学习

- [Vue 3 官方文档](https://cn.vuejs.org/)
- [Vue Router 文档](https://router.vuejs.org/zh/)
- [Composition API 指南](https://cn.vuejs.org/guide/extras/composition-api-faq.html)

### Tailwind CSS 学习

- [Tailwind CSS 官方文档](https://tailwindcss.com/docs)
- [Tailwind CSS 中文文档](https://www.tailwindcss.cn/docs)

### 其他工具

- [Vite 文档](https://cn.vitejs.dev/)
- [Express 文档](https://expressjs.com/)

## 🐛 常见问题

### 端口被占用

如果 3000 或 3001 端口被占用，可以修改：

**前端端口**：编辑 `frontend/vite.config.js`

```js
server: {
  port: 3000, // 修改为你想要的端口
}
```

**后端端口**：编辑 `backend/.env` 或 `backend/server.js`

```js
const PORT = process.env.PORT || 3001; // 修改默认端口
```

### 跨域问题

项目已配置 CORS，如果遇到跨域问题，检查：

1. 后端 `server.js` 中是否启用了 `cors` 中间件
2. 前端 `vite.config.js` 中的代理配置是否正确

### Tailwind CSS 样式不生效

1. 确保已安装依赖：`npm install`
2. 检查 `tailwind.config.js` 中的 `content` 路径是否正确
3. 确保 `src/style.css` 中引入了 Tailwind 指令

